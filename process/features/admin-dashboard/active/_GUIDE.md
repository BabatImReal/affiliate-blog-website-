# Admin Dashboard — Active Plans

In-progress implementation plans for the Admin Dashboard feature.

This feature covers: admin login page, Supabase Auth session management, admin route middleware, blog post editor (rich text), media library (Cloudinary upload), affiliate link management UI, analytics dashboard (views chart, clicks chart, revenue proxy table).

Naming: `admin-[task]_PLAN_[dd-mm-yy].md`
